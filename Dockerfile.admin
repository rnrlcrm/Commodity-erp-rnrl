FROM python:3.11-slim

WORKDIR /app

# Install dependencies (no build tools needed for pbkdf2)
RUN pip install --upgrade pip
RUN pip install passlib asyncpg

# Copy the admin creation script
COPY create_admin_direct.py /app/

# Run the script
CMD ["python3", "/app/create_admin_direct.py"]
